#include <stdio.h>

int reverse(char str);
int is_blank

int main()
{
    int c;

    while((c = getchar()) != EOF) {
        // Each letters
        if(is_)

    }

}

int reverse(char str) 
{

}

int not_blank(char c)
{
    return ! (c == '\t' || c == ' ' || c == '\n');
}
